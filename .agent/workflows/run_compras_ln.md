---
description: Run the Compras LN processing pipeline to transform the Excel file. This reads the input file from the project's Input folder, processes it, and generates the final report in the Output folder.
---
To execute the Compras LN pipeline:

1. Navigate to the project directory
// turbo
2. Run the main orchestrator script

```bash
cd "c:\Users\Tuchi\MiEstudioIA\Procesamiento archivo de compras LN"
python main.py
```
